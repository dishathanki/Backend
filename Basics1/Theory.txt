Topics 
    1. Basics of Node.js
    2. Intro to it
    3. Installing it
        node & npm
        working with node and npm
        npm init
        node basic usage
     4. working with modules.
     5. file system operations.
     6. understanding HTTP module

 what is Node.js
     basically node.js is javascript runtime environment 

 const fs = require('fs');

 writing into file through fs module
     fs.writeFile("hey.txt","hey how are you",function(err){
         if(err) console.log(err);
         else console.log("done");
     })

 appending data into the file
     fs.appendFile("hey.txt","hey how are you what r you doing ?",function(err){
         if(err) console.log(err);
         else console.log("done");
     })

 Renaming the file
     fs.rename("hey.txt","Hello.txt",function(err){
         if(err) console.log(err);
         else console.log("done");
     })

 copy filee
     fs.copyFile("Hello.txt","./Copy/Copy1.txt",function(err){
         if(err) console.log(err);
         else console.log("done");
      })

 Deleting the filee -: unlink
     fs.unlink("Hello.txt",function(err){
         if(err) console.log(err);
         else console.log("done");    
     })

 remove directory by default it can only remove empty folder to remove folder which have files in it we have to do recursive:true
     fs.rm("./Copy",{recursive:true},function(err){
         if(err) console.log("removed");
         else console.log("Removed");
     })

NPM Understanding   
    NPM - Node Package Manager
    NPM - no full form it covers or hold everything
    Things which installed by default with NODE.JS are called modules
    Things which install from NPM are called packages
    Dependensies - packages we install to use in our project
    Devdependensies = kind of dependensies which work only at development time 

Express.js Framework:

    Introduction - It is NPM package 
    It is responsilbe to manage everything from receiving the reuqest and giving the response 
    Setting up basic Express application -
    Routing -

    Middleware - 
    Request and Response handling - 
    Error handling -

Form handling , Sessions and Cookies :-
    session and Cookies